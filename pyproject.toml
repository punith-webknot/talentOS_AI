[project]
name = "talentos-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Used by every agent (supervisor, jd_creation, interview_scheduler, ...)
    "langgraph>=0.2.34",
    "langchain-core>=0.3.10",
    "langchain-openai>=0.2.2",
    # Required by jd_creation_agent — MCP integration for tool hosting.
    "langchain-mcp-adapters>=0.1.0",
    # Pydantic models in api/schemas.py and config/settings.py
    "pydantic>=2.9.2",
    "pydantic-settings>=2.5.2",
    # main.py / supervisor_agent.py
    "python-dotenv>=1.0.1",
]
